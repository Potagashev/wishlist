export enum ButtonVariant {
    pagination = "pagination",
    default = "default",
    rounded = "rounded",
    submit = "submit",
    disabled = "disabled",
}

export enum ButtonTypes {
    submit = "submit",
}
